# Core PyTorch (CPU version, recommended stable in 2025-2026)
torch>=2.0.0
torchaudio>=2.0.0

# SpeechBrain from develop branch (最新修复 + 模型支持)
#git+https://github.com/speechbrain/speechbrain.git@develop

# Hugging Face ecosystem (固定版本避免 AutoModelWithLMHead 等旧类错误)
transformers==4.35.2
huggingface_hub>=0.20.0

# Audio I/O and processing (绕过 torchaudio backend 问题)
soundfile>=0.12.0

# Microphone input
pyaudio>=0.2.11

# Text sentiment (你的代码用了)
textblob>=0.18.0.post0

# 其他常用依赖（requests 用于模型下载，numpy/torch 已包含在上面）
requests>=2.28.0
numpy>=1.23.0
scipy>=1.10.0